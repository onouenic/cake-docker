version: '3.8'
services:
  app:
    restart: always
    container_name: cursos-e-eventos-docker
    build:
      context: .
      dockerfile: Dockerfile
    # user: "1001:1001"
    # extra_hosts:
    # ports:
    volumes:
      - /opt/sites/cursos-e-eventos/www/:/var/www/html/
      - /opt/sites/:/opt/sites/
    command: sh -c "usermod --non-unique --uid 1001 www-data && groupmod --non-unique --gid 1001 www-data && chown -R www-data:www-data /var/www/html/ && apache2-foreground && cd app/Plugin/AuditoriaNIC && git submodule init && git submodule update"

networks:
  default:
    external:
      name: internal
