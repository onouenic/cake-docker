version: '3.8'
services:
  app:
    restart: always
    container_name: comunic-docker
    build:
      context: .
      dockerfile: Dockerfile
    user: root:root
    ports:
      - '91:80'

networks:
  default:
    external:
      name: internal
